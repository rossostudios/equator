// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Needed for canonical URLs, Open Graph and the sitemap. Change this the day
  // a custom domain is pointed at the project.
  site: 'https://equator-two.vercel.app',
});
